# LottoPro AI v2.0 - 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# 개발환경과 프로덕션환경에 따라 적절히 조정하세요

# === 기본 애플리케이션 설정 ===
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=your-super-secret-key-change-this-in-production
APP_HOST=0.0.0.0
APP_PORT=5000

# === 데이터 파일 설정 ===
CSV_PATH=new_1186.csv

# === 데이터베이스 설정 ===
DATABASE_URL=sqlite:///data/lottopro.db
DATABASE_POOL_SIZE=10
DATABASE_POOL_TIMEOUT=30

# === 투명성 및 컴플라이언스 설정 ===
TRANSPARENCY_MODE=enabled
DATA_RETENTION_DAYS=365
AUDIT_LOG_ENABLED=true
STATISTICAL_VALIDATION=strict

# === 보안 설정 ===
# HTTPS 환경에서만 SESSION_COOKIE_SECURE=true로 설정하세요
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax
WTF_CSRF_ENABLED=true

# === Rate Limiting 설정 ===
# 초기에는 메모리 기반, Redis 설치 후 redis://redis:6379로 변경
RATELIMIT_STORAGE_URL=memory://
RATELIMIT_DEFAULT=100 per hour
RATELIMIT_API=50 per hour

# === 캐시 설정 ===
# 초기에는 simple, Redis 설치 후 redis로 변경
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300
# Redis 사용시 아래 라인 활성화
# CACHE_REDIS_URL=redis://redis:6379/1

# === 로깅 설정 ===
LOG_LEVEL=INFO
LOG_FILE=logs/lottopro.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5

# === 모니터링 설정 ===
# 개발환경에서는 false, 프로덕션에서는 true 권장
MONITORING_ENABLED=false
MONITORING_PORT=5001
PROMETHEUS_METRICS=false
HEALTH_CHECK_INTERVAL=30

# === AI 모델 설정 ===
MODEL_UPDATE_INTERVAL=weekly
PREDICTION_HISTORY_LIMIT=1000
STATISTICAL_SIGNIFICANCE_LEVEL=0.05

# === 외부 서비스 설정 ===
LOTTERY_DATA_SOURCE=official_api
DATA_UPDATE_SCHEDULE=0 2 * * 6  # 매주 토요일 오전 2시

# === 알림 설정 ===
ALERT_EMAIL_ENABLED=false
# 이메일 알림 사용시 아래 설정들을 활성화하세요
# ALERT_EMAIL_SMTP_SERVER=smtp.gmail.com
# ALERT_EMAIL_PORT=587
# ALERT_EMAIL_USERNAME=your-email@gmail.com
# ALERT_EMAIL_PASSWORD=your-app-password
# ALERT_EMAIL_RECIPIENTS=admin@yourdomain.com

# === 백업 설정 ===
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 3 * * *  # 매일 오전 3시
BACKUP_RETENTION_DAYS=30
BACKUP_LOCATION=/app/backups

# === 법적 준수 설정 ===
AGE_VERIFICATION_REQUIRED=true
GAMBLING_WARNING_ENABLED=true
JURISDICTION=KR
COMPLIANCE_MODE=strict

# === 성능 최적화 ===
WORKERS=2
WORKER_CONNECTIONS=1000
WORKER_TIMEOUT=120
PRELOAD_APP=true

# === 개발 환경에서 사용할 설정들 ===
# 개발시에는 아래 설정들로 변경하세요:
# FLASK_ENV=development
# FLASK_DEBUG=True
# LOG_LEVEL=DEBUG
# SESSION_COOKIE_SECURE=false
# MONITORING_ENABLED=false
# TRANSPARENCY_MODE=disabled
# AUDIT_LOG_ENABLED=false

# === PWA 설정 (선택사항) ===
# PWA_NAME=LottoPro AI v2.0
# PWA_SHORT_NAME=LottoPro AI

# === API 키 설정 (선택사항) ===
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key

# === 분석 도구 설정 (선택사항) ===
# GOOGLE_ANALYTICS_ID=UA-XXXXXXX-X
# SENTRY_DSN=https://your-sentry-dsn

# === 파일 업로드 제한 (선택사항) ===
# MAX_CONTENT_LENGTH=16777216  # 16MB

# === 세션 설정 (선택사항) ===
# SESSION_PERMANENT=False
# PERMANENT_SESSION_LIFETIME=2592000  # 30 days

# === 추가 보안 헤더 (선택사항) ===
# DISABLE_ACCESS_LOG=False

# Redis 설정 (선택사항 - 없으면 메모리 캐시 사용)
REDIS_URL=redis://localhost:6379
# 또는 Redis Cloud 사용시
# REDIS_URL=redis://username:password@hostname:port

# 캐시 설정
DEFAULT_CACHE_TTL=300
CACHE_KEY_PREFIX=lottopro:v2:

# 모니터링 설정  
ENABLE_PERFORMANCE_MONITORING=true
PERFORMANCE_ALERT_EMAIL=admin@yoursite.com
