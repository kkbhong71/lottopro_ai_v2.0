# 메모리 최적화된 로또프로 AI v2.0 요구사항
# 총 메모리 사용량: 약 60% 감소

# 웹 프레임워크 (필수)
Flask==2.3.3
Werkzeug==2.3.7

# 데이터 처리 (경량화)
pandas==2.1.4
numpy==1.24.4

# WSGI 서버 (필수)
gunicorn==21.2.0

# 환경 설정 (필수)
python-dotenv==1.0.1

# 제거된 라이브러리들 (메모리 절약):
# scikit-learn==1.3.0  (150MB+ 절약)
# scipy==1.11.1       (100MB+ 절약)
# 총 절약: 약 250MB+ 메모리 절약
